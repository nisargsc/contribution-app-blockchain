## Blockchain API for the contribution app

This is the backend blockchain API for our contribution app.

## How to Run?
We use `pipenv` to manage our dependancies.

Go to the project directory and apply following commands.

```
pip install pipenv
pipenv install
pipenv run flask run
```